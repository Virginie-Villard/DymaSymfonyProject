<h1>{{ title }}</h1>

{# TODO :  add an image of the record #}

<p>
    Tracks :
</p>

<ul>
    {% for track in tracks %}
        <li>
            {{ track.song }} - {{ track.artist | upper }}
        </li>
    {% endfor %}
</ul>

